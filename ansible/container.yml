version: "1"
services:
  blsearch:
    image: centos:7
    ports:
      - "3000:3000"
      - "8983:8983"
    command: ["/usr/bin/dumb-init", "/bin/bash"]
    working_dir: "/var/www/blsearch"
    dev_overrides:
      environment:
        - "DEBUG=1"
      command: ["/usr/bin/dumb-init", '/bin/bash', '/app/ansible/sleep-loop.sh']
      volumes:
        - "$PWD/blsearch:/var/www/blsearch"
        - "$PWD:/app"
registries: {}
